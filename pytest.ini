[pytest]
# Only collect the template's own tests. The generated project lives under
# {{cookiecutter.repo_name}}/ as un-rendered Jinja, so it must never be collected.
testpaths = tests
norecursedirs = {{cookiecutter.repo_name}} generated
